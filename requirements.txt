# Core FastAPI dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
pydantic[email]>=2.0.0

# Database and ORM
SQLAlchemy>=2.0.0
alembic>=1.12.0
asyncpg>=0.27.0
psycopg[binary]>=3.1.0
psycopg2-binary>=2.9.0
pgvector>=0.2.0

# Authentication and Security
passlib[bcrypt]>=1.7.0
bcrypt==4.0.1  # Pin bcrypt to version compatible with passlib 1.7.x
pyjwt>=2.8.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.6

# Configuration and Environment
python-dotenv>=1.0.0

# ML and Embeddings
sentence-transformers>=2.2.0
numpy>=1.24.0
scikit-learn>=1.3.0
torch>=2.0.0
transformers>=4.21.0

# Redis
redis[asyncio]>=5.0.0
hiredis>=2.3.0
arq>=0.25.0

# Elasticsearch
elasticsearch[async]>=8.0.0,<9.0.0

# Utility libraries
requests>=2.28.0
httpx>=0.24.0
aiofiles>=23.0.0

# Image processing
Pillow>=10.0.0

# Document processing
PyPDF2>=3.0.0
pdfminer.six>=20231228
python-docx>=1.1.0
python-magic>=0.4.27

# NLP and Resume Parsing
spacy>=3.7.0
# spacy-transformers is only needed for TRF/LG SpaCy models (en_core_web_trf, es_core_news_lg).
# It is installed conditionally in the Dockerfile based on the SPACY_MODEL_* build args.
# Do not add it here — it pulls in the full CUDA torch when pip resolves the dependency tree.
langdetect>=1.0.9